# Resume file

Place the reviewed PDF at:

`public/documents/amrita-gautam-resume.pdf`

Then set `profile.resume.available` to `true` in `data/profile.ts`. The navigation, hero, and footer resume actions will automatically become PDF download links.

Before publishing, remove residential addresses, signatures, personal IDs, and any sensitive or unverifiable information.
